const hbg=document.getElementById('hamburger'),nl=document.getElementById('navlinks');hbg.addEventListener('click',()=>{nl.classList.toggle('open');hbg.setAttribute('aria-expanded',nl.classList.contains('open'));});nl.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>{nl.classList.remove('open');hbg.setAttribute('aria-expanded','false');}));function showTab(id,btn){document.querySelectorAll('.tab-panel').forEach(p=>p.classList.remove('active'));document.querySelectorAll('.tab').forEach(t=>{t.classList.remove('active');t.setAttribute('aria-selected','false');});document.getElementById('tab-'+id).classList.add('active');btn.classList.add('active');btn.setAttribute('aria-selected','true');}const form=document.getElementById('contactForm'),succ=document.getElementById('formSuccess');form.addEventListener('submit',async(e)=>{e.preventDefault();const btn=form.querySelector('.btn-submit');btn.textContent='Enviando...';btn.disabled=true;const data={nombre:document.getElementById('nombre').value,telefono:document.getElementById('telefono').value,clinica:document.getElementById('clinica').value,email:document.getElementById('email').value,edad:document.getElementById('edad').value,cargas:document.getElementById('cargas').value,prevision:document.getElementById('prevision').value,presupuesto:document.getElementById('presupuesto').value};try{const res=await fetch('https:method:'POST',headers:{'Content-Type':'application/json','Accept':'application/json'},body:JSON.stringify(data)});if(res.ok){form.style.display='none';succ.style.display='block';}else throw new Error();}catch{const msg=encodeURIComponent('Hola,quiero comparar mi plan ISAPRE. Nombre:'+data.nombre+' Telefono:'+data.telefono+' Clinica:'+(data.clinica||'no indicada')+' Prevision:'+(data.prevision||'no indicada')+' Cargas:'+(data.cargas||'no indicada'));window.open('https:form.style.display='none';succ.style.display='block';}});function animateCounters(){document.querySelectorAll('.stat-num,.hstat-num').forEach(el=>{const txt=el.textContent;const num=parseFloat(txt.replace(/[^0-9.]/g,''));if(isNaN(num))return;const prefix=txt.match(/^[^0-9]*/)[0];const suffix=txt.match(/[^0-9.]*$/)[0];let start=0,duration=1500,startTime=null;function step(ts){if(!startTime)startTime=ts;const prog=Math.min((ts-startTime)/duration,1);const ease=1-Math.pow(1-prog,3);el.textContent=prefix+Math.floor(ease*num)+suffix;if(prog<1)requestAnimationFrame(step);}requestAnimationFrame(step);});}const obs=new IntersectionObserver(entries=>{entries.forEach(e=>{if(e.isIntersecting){animateCounters();obs.disconnect();}});},{threshold:.3});obs.observe(document.querySelector('.stats-grid'));